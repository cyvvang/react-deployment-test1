import "./Favorites.css";

const Favorites = ({onClickFavoritesFilter}) => {
  return (
    <div className="square-btn" onClick={onClickFavoritesFilter}>
      <p className="large-text">❤️</p>
    </div>
  );
}

export default Favorites;